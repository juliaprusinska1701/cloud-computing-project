version: "3.8"

services:
  frontend:
    build: ./webapp
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=cache_db
    depends_on:
      - cache_db

  cache_db:
    image: redis:7-alpine
